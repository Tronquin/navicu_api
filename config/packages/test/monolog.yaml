monolog:
    channels: ['flight']
    handlers:
        flight:
            level:    debug
            type:     rotating_file
            path:     '%kernel.logs_dir%/flight/flight.log'
            max_files: 30
            channels: ["flight"]
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: error
            max_files: 30
            channels: ["!event"]
